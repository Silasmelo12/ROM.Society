server:
  port: 8081

spring:
  profiles:
    active: prod
  cloud:
    gcp:
      sql:
        enabled: false
      storage:
        enabled: true
        bucket: rom_society

  threads:
    virtual:
      enabled: true

  application:
    name: Club-Rom-Concept

api:
  security:
    token:
      secret: ${JWT_SECRET}

resend:
  api:
    key: ${RESEND_API_KEY}

totem:
  api:
    key: ${TOTEM_API_KEY}

