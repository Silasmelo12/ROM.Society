#version: '3.9'

services:
#  db:
#    image: postgres:15
#    container_name: postgres_container
##    restart: always
 #   environment:
 #     POSTGRES_USER: postgres
 #     POSTGRES_PASSWORD: postgres
 #     POSTGRES_DB: tetrix_db
 #   ports:
 #     - "5434:5432"
 #   volumes:
 #     - postgres_data:/var/lib/postgresql/data
 #   networks:
 #     - concept-net

  club_db:
    image: postgres:15
    container_name: club_rom_concept_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: concept
    ports:
      - "5433:5432" # Acessível no host pela porta 5432
    volumes:
      - club_postgres_data:/var/lib/postgresql/data
    networks:
      - concept-net

#  redis:
#    image: redis:7-alpine
#    container_name: redis_container
#    restart: always
#    networks:
#      - concept-net

#  evolution-api:
#    container_name: evolution_api
#    image: atendai/evolution-api:latest
#    environment:
#      - DOCKER_RESTART_POLICY=always
#      - SERVER_TYPE=http
#      - SERVER_PORT=8080
#      - CONFIG_SESSION_PHONE_VERSION=2.3000.1037225082
#    restart: always
#    ports:
#      - "8080:8080"
#    env_file:
#      - .env
#    depends_on:
#      - db
#      - redis
#    networks:
#      - concept-net

#  evolution_manager:
#    image: node:20
#    container_name: evolution_manager
#    restart: always
#    ports:
#      - "8081:8080"
#    environment:
#      # Apontando para a sua API internamente
#      - VITE_EVOLUTION_API_URL=http://evolution_api:8080

    # O comando mágico que resolve tudo isolado no Linux:
#    command: bash -c "rm -rf /app && git clone https://github.com/EvolutionAPI/evolution-manager-v2.git /app && cd /app && npm install && npm run build && npm run preview -- --host 0.0.0.0 --port 8080"
#    depends_on:
##      - evolution-api
#    networks:
#      - concept-net

volumes:
  postgres_data:
  club_postgres_data:

networks:
  concept-net:
    driver: bridge